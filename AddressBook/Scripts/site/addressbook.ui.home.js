var addressbook = window.addressbook || {};
addressbook.components = addressbook.components || {};
addressbook.components.newperson = addressbook.components.newperson || {};

addressbook.components.newperson = (function () {
    function init() {
        $('#create_new_person_btn').on('click', function () {
   
        });
    }
    return {
        init: init
    }
})();